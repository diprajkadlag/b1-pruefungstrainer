# Content pipeline: validation, PDF generation, audio synthesis.
# Python 3.11+ (developed on 3.14). All wheels are available for cp314/win_amd64.

# --- validation -------------------------------------------------------------
jsonschema>=4.23,<5

# --- PDF generation ---------------------------------------------------------
Jinja2>=3.1,<4

# --- audio synthesis --------------------------------------------------------
# Piper is the default TTS provider: offline, ONNX, redistributable voices.
piper-tts>=1.6,<2
onnxruntime>=1.20
numpy>=2.0
# libsndfile bindings. Handles MP3/OGG/FLAC/WAV encoding, so ffmpeg is not required.
soundfile>=0.13

# --- flashcard export -------------------------------------------------------
genanki>=0.13.1,<1

# --- development ------------------------------------------------------------
pytest>=8.0
ruff>=0.6
